# Source Note

These files are generated paper-craft concept assets prepared for PEELBEAST prototyping. They are isolated on a chroma-key background so production images can be swapped through `catalog.json` without changing game logic.
